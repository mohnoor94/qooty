import 'package:flutter/painting.dart';

class Styles {
  static const quoteTextStyle = TextStyle(
    fontSize: 30.0,
    fontFamily: 'Economica',
  );

  static const quotesScreenPadding = EdgeInsets.only(left: 20.0, right: 20.0, top: 20.0, bottom: 40.0);
}
